(function () {
  // тут ваш код
})(); // сразу же вызываем функцию
// без скобок вокруг объявления ф-ции js не поймет, что мы от него хотим
// так как ф-ция не имеет имени (анонимная), в window она не попадет

// IIFE - Immediately Invoked Function Expression - объявления и немедленный вызов
